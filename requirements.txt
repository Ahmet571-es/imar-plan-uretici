# ═══ Zorunlu (Streamlit Cloud'da yüklenmesi gereken) ═══
streamlit>=1.30.0
numpy>=1.24.0
pandas>=2.0.0
matplotlib>=3.7.0
shapely>=2.0.0
sqlalchemy>=2.0.0
Pillow>=10.0.0
requests>=2.31.0
plotly>=5.18.0
reportlab>=4.0.0

# ═══ Opsiyonel — yoksa demo/fallback modda çalışır ═══
folium>=0.15.0
streamlit-folium>=0.18.0

# NOT: anthropic ve openai requirements'tan çıkarıldı.
# Streamlit Cloud'da 1GB RAM sınırı var, bu paketler ~150MB.
# Tüm modüller API olmadan demo modda çalışır.
